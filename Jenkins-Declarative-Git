pipeline{
    agent any
  stages{
        stage("Git Checkout"){
            steps{
                git 'https://github.com/Vaishali0304/code.git'
            }
      }
        stage("Maven Build"){
   steps{
   withMaven(maven : 'MAVEN_HOME') 
       bat"mvn clean package"
   }
}
    }
}
